apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: device-api
spec:
  rules:
  - http:
      paths:
      - path: /api/v1/
        pathType: Prefix
        backend:
          service:
            name: device-api
            port:
              number: 9000