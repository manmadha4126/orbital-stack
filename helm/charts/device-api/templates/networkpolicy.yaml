apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: device-api
spec:
  podSelector:
    matchLabels:
      app: device-api
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          app.kubernetes.io/name: ingress-nginx